// Manipulando conteúdo

// innerText (texto interno)

const element = document.querySelector('h1')

element.innerText = "olá devs!"

//troca o texto interno da tag h1 que foi selecionada no queryselctor